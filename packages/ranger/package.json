{
  "name": "@sentinel-apex/ranger",
  "version": "0.1.0",
  "private": true,
  "description": "Ranger Earn integration layer - vault factory, strategy adapter, and on-chain vault lifecycle",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@sentinel-apex/domain": "workspace:*",
    "@sentinel-apex/shared": "workspace:*",
    "@sentinel-apex/observability": "workspace:*",
    "@solana/web3.js": "^1.98.4",
    "@solana/spl-token": "^0.4.0",
    "decimal.js": "^10.4.3",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
